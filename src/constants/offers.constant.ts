export enum OffersType {
    CATEGORY = "category",
    REFERAL = "referal"
}

export enum OfferStatus {
    ACTIVE = "active",
    PENDING = "pending",
    EXPIRED = "expired",
    BLOCKED = "blocked"
}

export enum OfferDiscountRange{
    MIN_RANGE = 100,
    MAX_RANGE = 1000
}